
const Button = ({ name }) => {
    return (
        <div>
            <button className="btn my-3 bg-amber-600 rounded-none text-white">{name}</button>
        </div>
    );
};

export default Button;